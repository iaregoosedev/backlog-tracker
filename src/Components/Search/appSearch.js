import Fuse from "fuse.js";
import React, { useState } from "react";
import appData from "../../Data/appList.json";

// export default function appSearch() {
//   const [results, setResults] = useState < Fuse.FuseResult < appData > [] > [];
//   const [search, setSearch] = useState('')

//   // const handleSearch= (text: string) => {
//   //   const fuse = new Fuse(appData, { keys: [`name`], setSearch(text); setResults(fuse.search(text).slice(0, 5));
//   }
// };
// }
